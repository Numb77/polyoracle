FROM python:3.12-slim

WORKDIR /app

# Install deps first (layer cache)
COPY pyproject.toml README.md ./
RUN pip install --no-cache-dir -e .

# Copy source
COPY . .

# Ensure logs directory exists
RUN mkdir -p logs

CMD ["python", "-m", "core.main", "--paper"]
